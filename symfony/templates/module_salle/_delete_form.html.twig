<form method="post" action="{{ path('module_salle_delete', {'id': module_salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ module_salle.id) }}">
    <button class="btn">Delete</button>
</form>