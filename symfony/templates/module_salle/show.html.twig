{% extends 'base.html.twig' %}

{% block title %}ModuleSalle{% endblock %}

{% block body %}
    <h1>ModuleSalle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ module_salle.id }}</td>
            </tr>
            <tr>
                <th>IdSalle</th>
                <td>{{ module_salle.idSalle }}</td>
            </tr>
            <tr>
                <th>IdModule</th>
                <td>{{ module_salle.idModule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('module_salle_index') }}">back to list</a>

    <a href="{{ path('module_salle_edit', {'id': module_salle.id}) }}">edit</a>

    {{ include('module_salle/_delete_form.html.twig') }}
{% endblock %}