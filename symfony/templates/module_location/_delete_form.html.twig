<form method="post" action="{{ path('module_location_delete', {'id': module_location.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ module_location.id) }}">
    <button class="btn">Delete</button>
</form>